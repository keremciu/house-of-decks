{
  "name": "node-against",
  "version": "1.0.0",
  "description": "api",
  "main": "server/index.js",
  "engines": {
    "node": "12.16.2"
  },
  "type": "module",
  "scripts": {
    "start": "node --experimental-modules server/index.js",
    "dev": "nodemon -- --experimental-modules server/index.js && (cd client && npm run watch)",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/keremciu/node-against-humanity.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/keremciu/node-against-humanity/issues"
  },
  "homepage": "https://github.com/keremciu/node-against-humanity#readme",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
